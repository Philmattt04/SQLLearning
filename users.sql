-- This collects the data from a table, which is called "users"
select * from users;
-- Using the query "count()", this takes the total number of rows in the column, which is "id"
select count(id) from users;
